# NeuroCad · Sprint 1 · Plan
**Version:** v0.1 · **Date:** 2026-04-07


**Rolling Plan (старт)**
```
1. NC-DEVOPS-INFRA-001 / DevOps    / Scaffold + окружение        / planned
2. NC-DEV-UI-001       / Developer / Workbench + Panel + Widgets  / planned
3. NC-DEV-CORE-001     / Developer / Context + Config + History   / planned
```

---

## Задачи

| Task Code | Роль | Фаза | Задача | Артефакт | Зависит от | Acceptance | Промт |
|---|---|---|---|---|---|---|---|
| **NC-DEVOPS-INFRA-001** | DevOps | 1 | Scaffold репозитория | pyproject.toml, DEV_SETUP.md, conftest.py, дерево файлов | — | `pip install -e .[dev]` чистый; `ruff check .` чистый; `mypy .` чистый; все файлы Sprint 1 из плана существуют; остальные модули ARCH допускаются как отсутствующие или заглушки | `TASK CODE: NC-DEVOPS-INFRA-001` / Создай структуру директорий по ARCH.md: neurocad/{__init__,InitGui,workbench}.py ui/{panel,widgets,settings}.py core/{context,history}.py config/{config,defaults}.py llm/{base,registry}.py tests/{test_context,test_history,test_config}.py / pyproject.toml: name=neurocad python>=3.11 deps=pydantic>=2.7,keyring>=25.0 dev=pytest>=8,pytest-qt,ruff,mypy / DEV_SETUP.md: платформа macOS arm64; FreeCAD 1.1.0 bundle Python 3.11.14; PYTHONPATH=/Applications/FreeCAD.app/Contents/Resources/lib:$PYTHONPATH; PySide6/shiboken6 брать из bundle не pip; symlink ~/Library/Application\ Support/FreeCAD/Mod/neurocad→./neurocad (macOS); НЕ ~/.FreeCAD/Mod / tests/conftest.py: QApplication fixture / InitGui.py содержит одну строку: import neurocad.workbench (FreeCAD GUI entrypoint) / НЕ делать: бизнес-логику, Docker, CI, cloud / Ответ без TASK CODE = невалиден |
| **NC-DEV-UI-001** | Developer | 2 | workbench.py + ui/widgets.py + ui/panel.py | workbench.py, widgets.py, panel.py, pytest-qt тест | NC-DEVOPS-INFRA-001 ✅ | QLineEdit (не QTextEdit); emit message_submitted и snapshot_requested; StatusDot.set_state() меняет цвет; MessageBubble разные цвета по role; сигналы ловятся pytest-qt | `TASK CODE: NC-DEV-UI-001` / workbench.py: CadCopilotWorkbench, MenuText="NeuroCad", Initialize→addDockWidget(Right), Activated/Deactivated show/hide / widgets.py: MessageBubble(role,text)→QFrame цвет по role; StatusDot→QLabel set_state(idle/thinking/error) / panel.py: CopilotPanel(QDockWidget), Signal message_submitted(str), Signal snapshot_requested(), _build_ui: QScrollArea+QLineEdit+QPushButton("Send")+QPushButton("Show Snapshot")+StatusDot в заголовке / НЕ делать: agent.run(), context.capture(), settings.py / Ответ без TASK CODE = невалиден |
| **NC-DEV-CORE-001** | Developer | 2 | core/context.py + config/config.py + core/history.py + тесты | context.py, config.py, history.py, test_*.py | NC-DEVOPS-INFRA-001 ✅ | save() не пишет api_key; load() фильтрует unknown keys; to_llm_messages() FEEDBACK→user; items() присутствует; to_prompt_str() ≤2000 символов при 20 объектах; тесты без реального FreeCAD | `TASK CODE: NC-DEV-CORE-001` / context.py: @dataclass ObjectInfo(name,type_id,shape_type\|None,volume_mm3\|None,placement,visible); @dataclass DocSnapshot(filename,active_object\|None,objects,unit="mm"); capture(doc)→DocSnapshot защита от отсутствия Shape; to_prompt_str(snap)→str ≤2000 символов / config.py: CONFIG_PATH=~/.freecad/neurocad/config.json; DEFAULTS(provider,model,base_url,max_tokens,temperature); load()→DEFAULTS+merge+strip api_key+filter unknown keys; save(cfg)→strip api_key перед записью; save_api_key→thin wrapper над keyring.set_password; UI не подключаем в этом спринте / history.py: Role(StrEnum) USER/ASSISTANT/FEEDBACK; @dataclass Turn(role,content); History.add(), to_llm_messages() FEEDBACK→"user", items()→list[Turn], clear() / Тесты: mock FreeCAD без реального GUI / НЕ делать: LLM вызовы, подключение к панели / Ответ без TASK CODE = невалиден |
| **NC-DEV-CORE-002** | Developer | 3 | Интеграция snapshot_requested→context.capture() в panel.py | panel.py (обновлён), интеграционный тест | NC-DEV-UI-001 ✅ NC-DEV-CORE-001 ✅ | Show Snapshot→текст в MessageBubble; нет крэша при пустом doc; тест зелёный | `TASK CODE: NC-DEV-CORE-002` / В CopilotPanel подключить: def _on_snapshot_requested(self): snap=context.capture(FreeCAD.ActiveDocument); self._add_message("assistant", context.to_prompt_str(snap)) / Защита при ActiveDocument=None→пустой snapshot без крэша / Тест: mock doc + нажатие Show Snapshot → текст появляется в scroll area / НЕ делать: agent.run(), LLM вызовы / Ответ без TASK CODE = невалиден |
| **NC-DEVOPS-INFRA-002** | DevOps | 4 | Финальный CI-прогон | stdout всех команд, вердикт PASS/FAIL | NC-DEV-CORE-002 ✅ | 0 failed; все 4 команды без ошибок | `TASK CODE: NC-DEVOPS-INFRA-002` / Запусти и верни полный stdout: pip install -e .[dev]; ruff check .; mypy .; pytest --tb=short -v / Вердикт: PASS если всё чисто, FAIL с указанием конкретных ошибок / Ответ без TASK CODE = невалиден |
| **NC-PM-REVIEW-001** | PM | 4 | DoD-чеклист: 14 пунктов | Закрытый чеклист | NC-DEVOPS-INFRA-002 ✅ | Все 14 пунктов approved; pytest зелёный подтверждён через NC-DEVOPS-INFRA-002 | Ручная проверка: (1) Workbench в меню FreeCAD, Report view чистый (2) QLineEdit→emit message_submitted (3) MessageBubble и StatusDot рендерятся (4) Show Snapshot→текст в чате (5) capture() пустой doc (6) capture() непустой doc (7) to_prompt_str()≤2000 символов (8) save() не пишет api_key (9) to_llm_messages() FEEDBACK→user (10) pip install -e .[dev] чистый (11) ruff check . чистый (12) mypy . чистый (13) pytest --tb=short 0 failed (14) FreeCAD→Box→Show Snapshot→Box виден в тексте |

---

**Правила останова** (встроены в каждый промт): ответ без TASK CODE = невалиден / Docker/CI/cloud → остановить / убирает api_key защиту или items() → rejected / противоречие ARCH.md → приоритет у файлов.
