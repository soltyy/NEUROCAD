# NeuroCad · Sprint 3 · Plan
**Version:** v0.1 · **Date:** 2026-04-07


**Предусловие: Sprint 2 DoD закрыт полностью.**

**Rolling Plan (старт)**
```
1. NC-DEV-UI-003      / Developer / Settings UI + ProgressBar                  / planned
2. NC-DEV-CORE-004    / Developer / Exporter + AgentCallbacks + stream path    / planned
3. NC-DEV-TEST-001    / Developer / Benchmark (tests/benchmark.py)             / planned
// Фаза 1: параллельно по контракту ARCH; сходятся в _on_message_submitted — без последовательного handoff, но с общим зафиксированным интерфейсом.
```

---

## Задачи

| Task Code | Роль | Фаза | Задача | Артефакт | Зависит от | Acceptance | Промт |
|---|---|---|---|---|---|---|---|
| **NC-DEV-UI-003** | Developer | 1 | Settings UI: полная логика (Save→keyring, Use once→session) + ProgressBar в ui/widgets.py | settings.py (полный), widgets.py (ProgressBar), test_config.py, test_adapters.py | Sprint 2 ✅ | Save: ключ в keyring, в config.json отсутствует; Use once: keyring не модифицируется; Base URL disabled для anthropic/openai, enabled для остальных; смена провайдера без перезапуска FreeCAD; невалидный ключ → ошибка в диалоге, не крэш; ProgressBar: spinner на 1й попытке, "Попытка 2/3" при retry через AgentCallbacks.on_attempt, idle/error через set_state | `TASK CODE: NC-DEV-UI-003` / ui/settings.py — полная логика: _on_save(): config.save(cfg без api_key)+save_api_key(provider,key)+self._adapter=registry.load_adapter(config.load())+self.accept(); _on_use_once(): self._adapter=registry.load_adapter_with_session_key(cfg,key)+self.accept(); Base URL QLineEdit disabled для anthropic/openai enabled для остальных; невалидный ключ → QMessageBox ошибка не крэш / ui/widgets.py — добавить ProgressBar(QWidget): set_state(Literal["idle","thinking","error"]); set_attempt(attempt,max_attempts) показывает "Попытка N/M"; StatusDot и ProgressBar управляются из одной точки _on_message_submitted / НЕ делать: streaming UI path здесь (NC-DEV-CORE-004), exporter / Ответ без TASK CODE = невалиден |
| **NC-DEV-CORE-004** | Developer | 1 | core/exporter.py + AgentCallbacks в agent.py + streaming UI path в panel.py | exporter.py, agent.py (обновлён), panel.py (обновлён), test_exporter.py | Sprint 2 ✅ | export(step) создаёт валидный STEP; export(stl) создаёт валидный STL; пустой список объектов → ValueError; неподдерживаемый формат → ValueError; текст появляется по мере генерации; UI не фризит; rollback работает в том же flow; ProgressBar синхронизирован с StatusDot из одной точки | `TASK CODE: NC-DEV-CORE-004` / core/exporter.py: SUPPORTED={"step","stl"}; export(doc,object_names,path): проверить fmt, собрать shapes через hasattr(obj,"Shape"), Part.makeCompound(shapes).exportStep/exportStl; пустой список → ValueError; неподдерживаемый формат → ValueError / Кнопка Export в panel.py: QFileDialog → exporter.export() / core/agent.py — добавить AgentCallbacks: @dataclass AgentCallbacks(on_chunk:Callable[[str],None], on_attempt:Callable[[int,int],None]); расширить run(...,callbacks:AgentCallbacks|None=None): if callbacks: callbacks.on_attempt(attempt+1,MAX_RETRIES); streaming через adapter.stream(); callbacks.on_chunk(chunk) / ui/panel.py — _on_message_submitted: status_dot.set_state("thinking")+progress_bar.set_state("thinking")+bubble=_add_message("assistant",""); cb=AgentCallbacks(on_chunk=lambda c:(bubble.append_text(c),QApplication.processEvents()), on_attempt=lambda n,mx:progress_bar.set_attempt(n,mx)); result=agent.run(...,cb); state="idle" if result.ok else "error"; status_dot.set_state(state)+progress_bar.set_state(state) / НЕ делать: Settings UI логику, benchmark / Ответ без TASK CODE = невалиден |
| **NC-DEV-TEST-001** | Developer | 2 | tests/benchmark.py: 50 задач, benchmark_results.json, отчёт по трём bucket | benchmark.py, benchmark_results.json, отчёт | NC-DEV-CORE-004 ✅ | benchmark прогоняется без крэша на 50 задачах; результаты в benchmark_results.json; отчёт содержит success rate по всем трём bucket + p90 latency + rollback count; простые ≥80% с 1й попытки; средние ≥70% за ≤3 попытки | `TASK CODE: NC-DEV-TEST-001` / tests/benchmark.py: @dataclass BenchmarkCase(prompt,expected_type,max_retries,latency_p90_ms); @dataclass BenchmarkResult(case,ok,attempts,latency_ms,geometry_valid,rollback_triggered,error|None); run_benchmark(adapter,doc,cases)→list[BenchmarkResult]; dataset: 20 простых/20 средних/10 сложных задач; результаты → benchmark_results.json; итоговый print-отчёт: success rate по каждому bucket, p90 latency, rollback count / Запускается вручную (не в pytest), требует реального LLM ключа и FreeCAD / НЕ делать: включать в pytest suite / Ответ без TASK CODE = невалиден |
| **NC-PM-DOG-001** | PM | 3 | Dog-food тест: ручная сессия FreeCAD от открытия до экспорта | Закрытый чеклист + фиксация метрик | NC-DEVOPS-INFRA-004 ✅ | Все 8 пунктов чеклиста пройдены; число промптов, retry и оценка качества зафиксированы | Ручная сессия: (1) Открыть FreeCAD, активировать NeuroCad workbench (2) Настроить провайдера через Settings UI — Save path (3) Создать корпус 50×30×10мм с двумя отверстиями через промпт (4) Добавить фаску 1мм на верхних рёбрах через промпт (5) Намеренно ввести некорректный промпт — убедиться в rollback (6) Сменить провайдера через Settings UI — Use once — убедиться в переключении (7) Экспортировать модель в STEP и STL (8) Проверить STEP в стороннем просмотрщике (FreeCAD reopen или KiCad). Зафиксировать: число промптов до финальной модели, число автоматических retry, субъективная оценка качества по провайдерам |
| **NC-DEVOPS-INFRA-004** | DevOps | 3 | Финальный CI-прогон Sprint 3 | stdout всех команд, вердикт PASS/FAIL | NC-DEV-UI-003 ✅ NC-DEV-CORE-004 ✅ NC-DEV-TEST-001 ✅ | ruff/mypy/pytest чистые; benchmark не в pytest | `TASK CODE: NC-DEVOPS-INFRA-004` / Запусти и верни полный stdout: ruff check .; mypy .; pytest --tb=short -v / benchmark.py НЕ включать в pytest — он запускается вручную / Вердикт: PASS если всё чисто, FAIL с конкретными ошибками / Ответ без TASK CODE = невалиден |
| **NC-PM-REVIEW-003** | PM | 4 | DoD-чеклист: 13 пунктов | Закрытый чеклист | NC-PM-DOG-001 ✅ | Все 13 пунктов approved | Проверить: (1) Settings UI Save → keyring, Use once → не пишет (2) Смена провайдера без перезапуска FreeCAD (3) Streaming: текст по мере генерации, UI не фризит (4) Export STEP и STL валидные файлы (5) ProgressBar показывает номер попытки при retry (6) Benchmark 50 задач без крэша, результаты в benchmark_results.json (7) Отчёт содержит success rate по всем трём bucket (8) Отчёт содержит p90 latency и rollback count (9) Простые задачи ≥80% с 1й попытки (10) Средние задачи ≥70% за ≤3 попытки (11) ruff check . чистый (12) mypy . чистый (13) pytest зелёный |

---

**Правила останова** (встроены в каждый промт): ответ без TASK CODE = невалиден / Addon Manager/мультимодальность/PartDesign → post-MVP, стоп / убирает AgentCallbacks или rollback → rejected / StatusDot и ProgressBar управляются не из одной точки → rejected / benchmark попадает в pytest suite → остановить / противоречие ARCH.md → приоритет у файлов.
