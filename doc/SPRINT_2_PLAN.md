# NeuroCad · Sprint 2 · Plan
**Version:** v0.1 · **Date:** 2026-04-07


**Предусловие: Sprint 1 DoD закрыт полностью.**

**Rolling Plan (старт)**
```
1. NC-DEV-LLM-001   / Developer / LLM Adapter                          / planned
2. NC-DEV-CORE-003   / Developer / Executor + Validator + Agent + Prompt / planned
3. NC-DEV-UI-002     / Developer / Интеграция agent.run() с панелью     / planned
```

---

## Задачи

| Task Code | Роль | Фаза | Задача | Артефакт | Зависит от | Acceptance | Промт |
|---|---|---|---|---|---|---|---|
| **NC-DEV-LLM-001** | Developer | 1 | LLM Adapter: llm/base.py (LLMResponse, LLMAdapter Protocol) + llm/anthropic.py (AnthropicAdapter) + llm/openai.py (OpenAIAdapter, base_url) + llm/registry.py (load_adapter, load_adapter_with_session_key) | base.py, anthropic.py, openai.py, registry.py, test_adapters.py | Sprint 1 ✅ | OpenAIAdapter с кастомным base_url инициализируется без ошибок; load_adapter() без ключа → ValueError; load_adapter_with_session_key() не вызывает keyring; test_session_key_not_persisted зелёный; mock-адаптер возвращает корректный LLMResponse (используется в NC-DEV-CORE-003 и тестах интеграции панели); интеграционные тесты skip без ключа | `TASK CODE: NC-DEV-LLM-001` / Реализуй LLM adapter layer. llm/base.py: @dataclass LLMResponse(content,input_tokens,output_tokens,stop_reason); class LLMAdapter(Protocol) def complete(messages,system,tools=None)→LLMResponse; def stream(messages,system)→Iterator[str] / llm/anthropic.py: AnthropicAdapter(api_key,model,max_tokens,temperature) / llm/openai.py: OpenAIAdapter(api_key,model,base_url,max_tokens,temperature) — покрывает openai/ollama/openai-compatible / llm/registry.py: ADAPTERS dict; load_adapter(config)→_resolve_api_key(env>keyring; при отсутствии ValueError); load_adapter_with_session_key(config,session_key)→адаптер без записи в keyring / test_adapters.py: mock-адаптер для unit-тестов; test_session_key_not_persisted; skip интеграционных без ключа / НЕ делать: Settings UI, UI streaming path (Sprint 3), реальные LLM вызовы в unit-тестах / Ответ без TASK CODE = невалиден |
| **NC-DEV-CORE-003** | Developer | 1 | core/executor.py (sandbox exec) + core/validator.py (геометрия) + core/agent.py (_execute_with_rollback + run()) + core/prompt.py (build_system) | executor.py, validator.py, agent.py, prompt.py, test_executor.py, test_validator.py, test_agent.py | Sprint 1 ✅ | execute() блокирует os/import/fenced/\_\_import\_\_/timeout/лимит объектов; _execute_with_rollback commit при успехе, abort при любой ошибке; документ не меняется при сбое; run() ≤3 общих попытки; history.items()==[USER,ASSISTANT] после успеха; history.items()==[USER,FEEDBACK,ASSISTANT] при успехе на 2й; to_llm_messages() FEEDBACK→user | `TASK CODE: NC-DEV-CORE-003` / core/executor.py: WHITELIST={"FreeCAD","App","Base","Part","math","json"}; @dataclass ExecResult(ok,error,traceback,new_objects,rolled_back=False); execute(code,doc)→ExecResult; timeout=10сек; макс новых объектов=5; блокировать import/from statements, fenced code blocks, os/sys/subprocess/open/eval/exec/\_\_import\_\_ / core/validator.py: @dataclass ValidationResult(ok,reason); check(doc,object_names)→ValidationResult; проверять Shape.isValid(), not isNull(), ShapeType in Solid/Shell/Compound/Face / core/agent.py: _execute_with_rollback(code,doc)→ExecResult через doc.openTransaction("NeuroCad")/commitTransaction/abortTransaction; import traceback внутри функции после docstring; AgentResult dataclass; run(user_message,doc,adapter,history:History)→AgentResult; MAX_RETRIES=3; history.add(USER) один раз; FEEDBACK отдельный turn; ASSISTANT только при успехе / core/prompt.py: DEFAULT_SYSTEM_PROMPT из config/defaults.py; build_system(snap)→str; to_llm_messages НЕ здесь — только в History / Тесты: mock FreeCAD, mock adapter / НЕ делать: UI подключение, streaming, Settings / Ответ без TASK CODE = невалиден |
| **NC-DEV-UI-002** | Developer | 2 | Подключить agent.run() к сигналу message_submitted в ui/panel.py | panel.py (обновлён), ручная проверка | NC-DEV-LLM-001 ✅ NC-DEV-CORE-003 ✅ | Ввод "создай куб 10×10×10мм" → Box в viewport; сломанный промпт → ошибка в панели, документ не тронут; StatusDot idle→thinking→idle/error | `TASK CODE: NC-DEV-UI-002` / В ui/panel.py подключить: def _on_message_submitted(self,text): self.status_dot.set_state("thinking"); self._add_message("user",text); result=agent.run(text,FreeCAD.ActiveDocument,self._adapter,self._history); if result.ok: self._add_message("assistant",result.code); self.status_dot.set_state("idle"); else: self._add_message("feedback",f"Ошибка: {result.error}"); self.status_dot.set_state("error") / self._adapter создаётся через load_adapter(config.load()) при инициализации панели / self._history = History() / Prerequisite ручной проверки: NEUROCAD_API_KEY env var + ~/.freecad/neurocad/config.json (provider, model) / НЕ делать: Settings UI, streaming, ProgressBar (Sprint 3) / Ответ без TASK CODE = невалиден |
| **NC-DEVOPS-INFRA-003** | DevOps | 3 | Финальный CI-прогон Sprint 2 | stdout всех команд, вердикт PASS/FAIL | NC-DEV-UI-002 ✅ | 0 failed; ruff/mypy/pytest чистые; интеграционные тесты skip без ключа | `TASK CODE: NC-DEVOPS-INFRA-003` / Запусти и верни полный stdout: ruff check .; mypy .; pytest --tb=short -v / Интеграционные тесты с реальным LLM — skip при отсутствии ключа, не fail / Вердикт: PASS если всё чисто, FAIL с конкретными ошибками / Ответ без TASK CODE = невалиден |
| **NC-PM-REVIEW-002** | PM | 3 | DoD-чеклист: 11 пунктов | Закрытый чеклист | NC-DEVOPS-INFRA-003 ✅ | Все 11 пунктов approved | Ручная проверка: (1) AnthropicAdapter и OpenAIAdapter возвращают LLMResponse (2) load_adapter() без ключа → ValueError (3) execute() блокирует os/import/fenced/timeout (4) _execute_with_rollback commit при успехе, abort при ошибке (5) документ не меняется при любом сбое (6) run() ≤3 попытки, history структурирована корректно (7) ввод промпта в панели → объект в viewport (8) сломанный промпт → ошибка в панели, документ не тронут (9) ruff check . чистый (10) mypy . чистый (11) pytest зелёный (интеграционные skip без ключа) |

---

**Правила останова** (встроены в каждый промт): ответ без TASK CODE = невалиден / UI streaming path/Settings UI/ProgressBar → это Sprint 3, стоп; метод stream() в адаптере разрешён (часть контракта ARCH) / убирает rollback или меняет History контракт → rejected / упрощает sandbox whitelist → rejected / противоречие ARCH.md → приоритет у файлов.
